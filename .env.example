# setup_aws.py copies this file to .env and fills it in.
# Don't put AWS keys in this file. Paste them into aws-credentials.txt and run setup_aws.py.
export AGENTCORE_HARNESS_ARN=
export AWS_REGION=us-east-1
